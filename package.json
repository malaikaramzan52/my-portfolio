{
  "name": "portfolio-mern",
  "version": "1.0.0",
  "description": "A dynamic MERN personal portfolio website with automatic database seeding and dynamic contact form handling.",
  "scripts": {
    "install-all": "npm install --prefix server && npm install --prefix client",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "dev": "powershell -Command \"Start-Process npm -ArgumentList 'run dev --prefix server'; Start-Process npm -ArgumentList 'run dev --prefix client'\""
  }
}
